apiVersion: v1
kind: Service
metadata:
  name: argocd-svc
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-server
spec:
  ports:
    - port: 443
      targetPort: 8080
      nodePort: 30443
      protocol: TCP
  selector:
    app.kubernetes.io/name: argocd-server
  type: NodePort
